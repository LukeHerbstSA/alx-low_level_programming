#include "main.h"

/**
	* main - Entry point
	* Description: This program tests the output of a called function
	* Return: Always 0 (Success)
	*/
int main(void)
{
	int num;

	num = 0;
	positive_or_negative(num);
	return (0);
}
