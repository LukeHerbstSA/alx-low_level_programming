#include <stdio.h>
/**
	* main - Entry point
	* Description: Uses putchar to print a string
	* Return: Always 0 (Success)
	*/
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
		putchar('0' + n);
	putchar('\n');
	return (0);
}
