#include <stdio.h>
/**
	* main - Entry point
	* Description: Uses putchar to print a string
	* Return: Always 0 (Success)
	*/
int main(void)
{
	int array;

	for (array = 0; 0 <= 9; array++)
		putchar('0' + array)
		putchar(', ')
	putchar('\n')
}
