#include <stdio.h>
/**
	* main - Entry point
	* Description: Uses printf to print a char string
	* Return: Always 0 (Success)
	*/
int main(void)
{
	char stringex2[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n" ,stringex2);
	return (0);
}

