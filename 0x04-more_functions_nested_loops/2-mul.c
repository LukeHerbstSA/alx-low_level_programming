#include "main.h"

/**
	* mul - int function
	* Description: returns the res of 2 multiplied numbers
	* @a: int type
	* @b: int type
	* Return: Always the result of the two integers
	*/
int mul(int a, int b);
{
	return (a * b);
}
