#include <stdio.h>
#include "main.h"
/**
	* main - Entry point
	* Description: program checks if c fits within upper or lower range
	* Return: Always 0 (Success)
	*/
int main(void)
{
	int _isalpha(int c);
	putchar('\n');
	return (0);
}
