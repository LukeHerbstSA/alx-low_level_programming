#include "main.h"

/**
	* int_islower(int c) - this will check if c is within the lowercase range
	* Parameter - c
	* Return: Always 0 (Success)
	*/
int islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	else
		return (0);
	putchar('\n')
	return (0);
}
